scalar Date

type Query {
    hello : String

    language (languageId: ID): LanguageDto

    languages (
        languageId: ID,
        name: String,
        lastUpdate: Date
    ) : [LanguageDto]

}
