scalar Date

type Query {
    hello : Message

    language (languageId: ID): LanguageResponse

    languages (
        name: String,
        lastUpdate: Date
    ) : [LanguageResponse]

    films : [FilmResponse]

    customers : [CustomerResponse]

}
